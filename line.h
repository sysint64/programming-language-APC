#ifndef JOBELL_LINE
#define JOBELL_LINE 1

static int lineno = 1; // Номер строки
static int posno  = 1; // Номер символа
//
static int curline  = 1;
static int curposno = 1;

void SetCurLine() {
	//curline = lineno;
	//posno = curposno;
}

#endif
